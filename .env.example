# Googleスプレッドシート用（ローカル開発時に .env にコピーして値を設定）
SPREADSHEET_ID=
SHEET_NAME=todos

# 方法A: JSONファイルのパス（そのまま貼り付け不要。ローカル向け）
# GOOGLE_SERVICE_ACCOUNT_JSON_PATH=/path/to/service-account.json

# 方法B: JSONを1行にした文字列（デプロイ時向け）
# GOOGLE_SERVICE_ACCOUNT_JSON=

# Flask（本番では必ず変更）
SECRET_KEY=dev-secret-key-change-in-production
